name: dev-env
on:
  push:
    branches:
      - dev
jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - run: docker build -t ghcr.io/supakkavee/bookinfo-ratings:dev .
      - run: docker images
      - run: docker push ghcr.io/supakkavee/bookinfo-ratings:dev
